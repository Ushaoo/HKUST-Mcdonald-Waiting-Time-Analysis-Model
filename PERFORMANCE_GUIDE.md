🚀 开发板性能优化指南
========================

## 当前情况分析

你的开发板上的性能指标：
- YOLOv8n: ~1580ms/帧 = **0.6 FPS**
- YOLOv5nu: ~1446ms/帧 = **0.7 FPS**

这解释了为什么实时视频这么卡顿！

## ✅ 已实施的优化（自动生效）

1. **分辨率降低**: 640x480 → 320x240
   - 减少计算量 4 倍
   
2. **增加跳帧间隔**: 6 → 15
   - 只每15帧检测一次（约每0.5秒检测一次）
   - 视频流畅度提升
   
3. **降低置信度阈值**: 0.5 → 0.3
   - 加快NMS（非极大值抑制）过程
   - 仍然能检测出人群
   
4. **显示推理时间**: 在视频上显示 "Inference: XXXms"
   - 实时监控性能指标

## 预期效果

**改进前**: 卡顿，每秒检测不到1次
**改进后**: 
- 视频流畅播放 (30 FPS)
- 每0.5秒更新一次人群检测
- CPU占用率降低

## 📊 优化的权衡

| 方面 | 优化前 | 优化后 |
|-----|------|------|
| 分辨率 | 640x480 | 320x240 |
| 检测频率 | 每3帧 | 每15帧 |
| 推理精度 | conf=0.5 | conf=0.3 |
| 视频流畅度 | 卡顿 | 流畅 |
| 人群检测精度 | 高 | 中等 |
| 实时性 | 低 | 高 |

## 🔧 进一步优化选项（按优先级）

### 选项 A: 切换更轻量模型 (推荐)
```bash
# 下载YOLOv5n模型
python3 -c "from ultralytics import YOLO; YOLO('yolov5n.pt')"

# 编辑 MC_web.py 第26行
# 将 model_name='yolov8n.pt'
# 改为 model_name='yolov5n.pt'
```
**预期收益**: 再提升 10-20% 速度
**风险**: 精度略低

### 选项 B: 进一步降低分辨率
```python
# MC_web.py 第30-31 行
self.cap.set(cv2.CAP_PROP_FRAME_WIDTH, 240)   # 改为240
self.cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 180)  # 改为180
```
**预期收益**: 再提升 30% 速度
**风险**: 画质下降，小细节丢失

### 选项 C: 增加跳帧间隔
```python
# MC_web.py 第37 行
self.frame_skip = 20  # 改为20（每20帧检测一次，约0.67秒）
```
**预期收益**: 速度再提升，但检测频率更低
**风险**: 人群变化反应延迟

### 选项 D: 使用导出优化的模型
```bash
# 将模型导出为ONNX格式（更快）
python3 -c "from ultralytics import YOLO; YOLO('yolov8n.pt').export(format='onnx')"
```
**预期收益**: 20-30% 性能提升
**复杂度**: 较高

## 性能监控

### 1. 查看实时推理时间
- 打开实时视频页面
- 右下角显示 "Inference: XXXms"
- 如果 > 1000ms，说明模型负载过高
- 如果 < 500ms，可以接受
- 如果 < 200ms，性能优秀

### 2. 查看CPU占用
```bash
# 在另一个终端运行
watch -n 1 'ps aux | grep MC_web | grep -v grep'
```

### 3. 监控内存使用
```bash
# 查看内存占用
free -h

# 监控YOLO进程
top -p $(pgrep -f MC_web.py)
```

## 视频处理 vs 实时视频

重要: **视频处理使用原始模型**，不受这些优化影响
- 上传视频仍使用 yolov8n.pt
- 精度和质量不变
- 速度较慢但结果准确

## 最终建议

### 如果你只想要流畅的实时视频：
```
当前优化已足够 ✓
```

### 如果你想要更快的检测速度：
```
1. 运行: python3 switch_config.py
2. 选择 "高性能模式"
3. 重启应用
```

### 如果你需要最高精度：
```
接受当前性能水平
或购买更强悍的硬件 🎮
```

## 常见问题

**Q: 为什么视频还是有点卡？**
A: 这是开发板的CPU限制。网页MJPEG流会额外消耗10-20%性能。
   可以用硬件加速编解码器改进（需要驱动支持）。

**Q: 人群检测数据准不准？**
A: 降低置信度后，假阳性会增加，但对密度估算影响不大。
   如果精度要求高，可以：
   - 增加 frame_skip（检测次数少，但准确率高）
   - 降低分辨率补偿（保持性能）

**Q: 能否同时优化实时视频和视频处理？**
A: 可以，但需要用不同的模型：
   ```python
   self.model = YOLO('yolov8n.pt')  # 实时视频用轻量版
   process_model = YOLO('yolov8m.pt')  # 视频处理用标准版
   ```

**Q: 为什么上传视频那么慢？**
A: 视频处理不受这些优化影响，仍使用高精度模型。
   这是必要的，确保保存的视频质量高。

## 总结

✅ 已优化实时视频流畅度
✅ 自动显示推理性能
✅ 保留视频处理精度
✅ 提供灵活的配置工具

你现在可以：
1. 享受流畅的实时视频预览
2. 获得准确的人群密度数据
3. 下载高质量的处理视频

祝使用愉快! 🎉
